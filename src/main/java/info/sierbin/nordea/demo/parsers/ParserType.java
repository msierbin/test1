package info.sierbin.nordea.demo.parsers;

public enum ParserType {
    NOOP,
    SENTENCE_SPLITTER,
    REMOVE_SPACES,
    XML_SENTENCE_PARSER,
    XML_PARSER,
    CSV_SENTENCE_PARSER,
    CSV_PARSER;
}
